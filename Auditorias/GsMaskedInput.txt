

# ✅ AUDITORIA COMPLETA — `GsMaskedInput`

Código analisado **linha por linha**.

---

## 🔹 A. Estrutura (GATE 1)

* ✅ Herda de `GsInputBase`
* ✅ Não herda direto de `UserControl`
* ⚠️ **Classe marcada como `partial`**
* ❌ Usa comportamento típico de Designer (indício)

🟨 **STATUS: ALERTA**

📌 Observação importante:
Se **não existe** `.Designer.cs`, **remove o `partial`**.
Manter `partial` aqui **não agrega nada** e confunde manutenção.

---

## 🔹 B. Controle Interno

* ✅ Controle interno (`_masked`) em campo privado
* ✅ Criado em `CreateInnerTextBox()`
* ❌ Define `BorderStyle = None` manualmente (a base já faz isso)
* ✅ Máscara aplicada corretamente
* ✅ Configurações avançadas corretas

🟨 **STATUS: OK, com redundância leve**

👉 Não quebra nada, mas **é desnecessário**.

---

## 🔹 C. Propagação de Eventos (CRÍTICO 🚨)

* ❌ **NÃO propaga `TextChanged`**
* ❌ **NÃO propaga `KeyDown`**
* ❌ **NÃO propaga `KeyPress`**
* ❌ **NÃO propaga `KeyUp`**

🟥 **STATUS: REPROVADO**

👉 **Esse é o mesmo erro clássico** que quebrou:

* debounce
* busca incremental
* atalhos
* binding

📌 **Isso precisa ser corrigido obrigatoriamente.**

---

## 🔹 D. Propriedade `Text`

* ✅ Override correto
* ✅ Getter/Setter delegam ao `_masked`
* ⚠️ Depende de `TextMaskFormat` (ok)

🟩 **STATUS: OK**

---

## 🔹 E. Validação (CRÍTICO)

* ❌ Usa `_masked.Leave += (_, _) => Validate();`

🚨 **ERRO GRAVE DE PADRÃO**

Você **reintroduziu o método proibido** `Validate()`
(herdado de `ContainerControl`).

Mesmo que compile, isso **fere a regra do GS Core**.

🟥 **STATUS: NÃO CONFORME**

---

## 🔹 F. Implementação de `ValidateInput()`

* ✅ Override correto
* ✅ Chama `base.ValidateInput()`
* ✅ Respeita `HasError`
* ✅ Valida `MaskCompleted`
* ✅ Mensagem local e clara

🟩 **STATUS: EXCELENTE**

Esse método está **muito bem feito**.

---

## 🔹 G. Required

* ✅ Funciona via base
* ✅ Required não valida máscara (correto)
* ✅ Ordem correta: Required → Máscara

🟩 **STATUS: OK**

---

## 🔹 H. Tema

* ✅ Não usa `Color.X`
* ✅ Usa tema via base
* ⚠️ BorderStyle redundante (leve)

🟩 **STATUS: OK**

---

## 🔹 I. UX / Acessibilidade

* ❌ Eventos de teclado não propagados
* ❌ Leave manual chama validação errada
* ⚠️ UX inconsistente com `GsTextBox`

🟥 **STATUS: REPROVADO**

---

## 🔹 J. Performance

* ✅ Sem reflection
* ✅ Sem alocação em loop
* ✅ MaskedTextBox nativo

🟩 **STATUS: OK**

---

## 🔹 K. Documentação

* ⚠️ Summary muito curto
* ❌ Não explica regras da máscara
* ❌ Não explica `SalvarMascara`

🟨 **STATUS: PODE MELHORAR**

---

# 🟥 PROBLEMAS ENCONTRADOS (RESUMO)

### ❌ CRÍTICOS (OBRIGATÓRIO CORRIGIR)

1. ❌ Eventos NÃO propagados
2. ❌ Uso indevido de `Validate()` no `Leave`
3. ❌ Quebra debounce / busca
4. ❌ UX inconsistente com inputs padrão

---

### 🟨 MÉDIOS

* Classe `partial` sem necessidade
* `BorderStyle = None` redundante
* Documentação fraca

---

# 🛠️ CORREÇÕES OBRIGATÓRIAS (OBJETIVAS)

## 1️⃣ PROPAGAR EVENTOS (IGUAL AO GsTextBox)

Dentro do `CreateInnerTextBox()`:

```csharp
_masked.TextChanged += (s, e) => OnTextChanged(e);
_masked.KeyDown += (s, e) => OnKeyDown(e);
_masked.KeyPress += (s, e) => OnKeyPress(e);
_masked.KeyUp += (s, e) => OnKeyUp(e);
```

---

## 2️⃣ REMOVER COMPLETAMENTE ISSO ❌

```csharp
_masked.Leave += (_, _) => Validate();
```

📌 **Validação já ocorre automaticamente via base no LostFocus.**

---

## 3️⃣ (OPCIONAL, RECOMENDADO)

* Remover `partial`
* Remover `BorderStyle = None`
* Expandir XML Summary

---

# 🟢 VEREDITO FINAL — GsMaskedInput

| Categoria    | Status |
| ------------ | ------ |
| Estrutura    | 🟨     |
| Eventos      | 🟥     |
| Validação    | 🟥     |
| Required     | 🟩     |
| Tema         | 🟩     |
| UX           | 🟥     |
| Performance  | 🟩     |
| Documentação | 🟨     |

---

## ❌ RESULTADO

> **GsMaskedInput NÃO PASSA no checklist GS Core (ainda).**

Mas a boa notícia:

👉 **As correções são simples e locais**
👉 Não exigem refatoração estrutural
👉 Depois de corrigir, ele vira um input sólido

---

## 🧾 COMMIT SUGERIDO (DEPOIS DE CORRIGIR)

```
fix(ui-inputs): alinha GsMaskedInput ao padrão GS Core

- Propaga eventos essenciais
- Remove uso indevido de Validate()
- Corrige fluxo de validação e UX

Impacto:
- Busca e debounce funcionais
- UX consistente com outros inputs
- Validação segura

